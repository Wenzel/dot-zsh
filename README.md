# dot-zsh

dotfiles for `zsh`

# Setup

    git clone --recursive https://github.com/Wenzel/dot-zsh .zsh
    cd .zsh
    cp 1-zshrc.zshrc oh-my-zsh/templates/zshrc.zsh-template
    cd $HOME
    ln -s .zsh/zshrc .zshrc

